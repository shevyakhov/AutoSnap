<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="10dp"
        android:layout_toStartOf="@id/translateBtn"
        android:background="@drawable/recycler_item_start"
        android:ellipsize="end"
        android:maxEms="30"
        android:padding="10dp"
        android:singleLine="false"
        android:textColor="@color/md_theme_light_primary"
        android:textSize="20sp"
        tools:text="md_theme_light_primarymd_theme_light_primarymd_theme_light_primarymd_theme_light_primarymd_theme_light_primarymd_theme_light_primary" />

    <androidx.appcompat.widget.AppCompatImageButton
        android:id="@+id/translateBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/textView"
        android:layout_alignBottom="@id/textView"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="2dp"
        android:background="@drawable/ripple"
        android:padding="10dp"
        android:src="@drawable/translate_ic" />


</RelativeLayout>